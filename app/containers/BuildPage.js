import React, { Component } from 'react';
import Build from '../views/Build';

type Props = {};

export default class BuildPage extends Component<Props> {
  props: Props;

  render() {
    return <Build />;
  }
}
